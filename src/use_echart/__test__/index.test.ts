import { describe, it, expect } from 'vitest';
import { useEChart } from '../index';

describe('useEChart', () => {
  it('useEChart defined ok', () => {
    expect(useEChart).toBeDefined();
  });
});
